// ToDo
// sm_cvar mp_gamemode realism // coop
// sm_cvar music_manager 0 // 1

// Test
// exec custom/test.cfg

// map c1m2_streets c5m3_cemetery
// sm plugins unload_all
// sm plugins refresh
// sm_refresh // Refresh plugins (same as "sm plugins refresh").
// sm_renew // Unload and Refresh plugins (same as "sm plugins unload_all; sm plugins refresh").
// sm_unload // Unload all plugins (same as "sm plugins unload_all").
// sm_reloadadmins
// sm_to_reload // Reloads the data config (Target Override)
// sm_info_reload // Reloads the mission and weapons configs. Weapons info data is re-parsed allowing changes to be made live without changing level.
// exec sourcemod/l4d2_loot.cfg

// sm_cvar nb_blind 1 // Disable vision
// sm_cvar nb_stop 1 // Stop all bots

// sm_cvar cl_showevents 1 // 0
// sm_cvar net_showevents 2 // 0

// director_force_panic_event
// sm_cvar sv_rescue_disabled 1 // 0

sv_cheats 1 // 0
sv_allow_lobby_connect_only 0 // 1
l4d2_l4lp_debug 1 // 0
l4d2_l4lp_min_difficulty 0 // 2
z_difficulty Easy // Normal, Hard, Impossible

director_stop // director_start
sm_cvar sb_stop 1 // Forces survivor bots to stand still

sm_cvar z_common_limit 0 // 30
sm_cvar z_minion_limit 0 // 3
l4d2_l4lp_infected_limit 0 // 4
l4d2_l4lp_incap_spawn_si 0 // 1

sm_cvar z_tank_health 1 // 4000
sm_cvar z_witch_health 1 // 1000